import { Routes } from '@angular/router';
import { FormularioComponent } from './tarea/formulario/formulario.component';

export const routes: Routes = [
    {
        path:'',
        component:FormularioComponent
    }
];
